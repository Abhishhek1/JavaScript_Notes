// if
const score = 200

if (score > 100) {
    let power = "fly"
    console.log(`user power: ${power}`);
}

// Comparision Operator:
// <, >, <=, >=, ==, !=, ===

// Switch Case

const month = 3;

switch (month) {
    case 1:
        
        break;
    
    case 2:
        
        break;

    case 3:
        console.log("March");
        break;

    default:
        break;
}

